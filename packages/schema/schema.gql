enum ReportType {
  OK
  WARN
  ERROR
}

type Report {
  id: ID!
  type: ReportType!
  html: String
  message: String
}

type Mutation {
  diagnoseURL(url: String!): [Report!]!
}

schema {
  mutation: Mutation
}
