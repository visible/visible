version: '3'

services:
  postgres:
    image: postgres:12.2
    volumes:
      - ./postgres:/var/lib/postgresql/data

  web:
    build: .
    image: visiblehq/web
    restart: always
    env_file: .env
    ports:
      - '3000:3000'
      - '4000:4000'
    depends_on:
      - postgres
    volumes:
      - ./logs:/home/packages/web-server/logs
